version: '3.8'

services:
  app:
    networks:
      - default
    build: .
    ports:
      - "3000:3000"
    command: sh ./wait-for-sql.sh && npm start
    volumes:
      - ./create_database.sql:/tmp/create_database.sql
      - .:/usr/src/app
      - /usr/src/app/node_modules
    depends_on:
      - mysql
    environment:
      - DATABASE_URL=${MYSQL_URL}
  mysql:
    image: mysql:latest
    environment:
      MYSQL_ROOT_PASSWORD: "${MYSQL_ROOT_PASSWORD}"
      MYSQL_DATABASE: "Taskies"
    ports:
      - "3306:3306"